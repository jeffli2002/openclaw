STATUS: ok
ROUTE_DEBUG: route=coding;dispatch=spawn;model=openai-code/gpt-5.4
DISPATCH_ID: 20260306-222758-coding-0f1aa9af
AGENT: coding

1. 风险点1：路由匹配过宽，存在误派 Bug。
- 位置：`scripts/agent_keyword_router.py:110-128`，`_rule_matches()` 采用双向子串匹配：`normalized_keyword in agent_kw` / `agent_kw in normalized_keyword`。
- 实际现象：我直接跑了这条任务，提取结果里出现了 `条修复建议`、`分析` 这类自然短语；其中 `条修复建议` 会因为包含“修复”被算进 coding 命中，`分析` 又会同时命中 growth/product。
- 影响：分数被虚高后，`scripts/chief_dispatch.py:145-199` 会基于这个结果直接走 `delegate_spawn`，导致错派、漏 triage，属于稳定性 Bug。

2. 风险点2：结果回传桥校验过弱，存在 API/结果污染风险。
- 位置：`scripts/wait_dispatch_result.py:54-70`。
- 问题：脚本只要看到 `path.exists()` 且 `st_size > 0` 就认定 `ready=True`，但没有校验：
  - 文件是否位于允许目录下
  - `STATUS / DISPATCH_ID / AGENT` 是否完整
  - `dispatch_id` 是否和当前任务一致
- 影响：部分写入文件、旧文件、错误文件都可能被当成成功结果；如果上层未来把 path 传错或被污染，这里还会退化成任意本地文件读取面。

修复建议（1条）：把 Chief 路由执行器改成“强校验契约”。
- 路由侧：改为 token/词边界级精确匹配，去掉 `agent_kw in normalized_keyword` 这类模糊命中，并加 stopwords（如“建议”“分析”等泛词降权）。
- 回传侧：`wait_dispatch_result.py` 增加 `--expected-dispatch-id`、`--expected-agent`、`--allowed-dir`，只有当文件路径合法且头部字段完整匹配时才返回 `ready=True`。

这 1 条改动能同时压住“误路由”和“误收结果”两个核心风险。