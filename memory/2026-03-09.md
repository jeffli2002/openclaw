# 2026-03-09

## Abi / Chief 协作文档
- 已创建并写入 3 份 Feishu 协作文档：
  - `[Agent协作] 06 阿比专用 Onboarding Prompt`
    - doc_token: `MPehd30hwoNQinxS0hVcJpWznHe`
    - url: `https://feishu.cn/docx/MPehd30hwoNQinxS0hVcJpWznHe`
  - `[Agent协作] 07 Chief 读取阿比协作摘要`
    - doc_token: `S5yUdPIkloevfmxwFIxc1S1Fnrf`
    - url: `https://feishu.cn/docx/S5yUdPIkloevfmxwFIxc1S1Fnrf`
  - `[Agent协作] 08 共享技能仓库建议结构`
    - doc_token: `R18jdOguWotO1QxcwWHc5jzmnvh`
    - url: `https://feishu.cn/docx/R18jdOguWotO1QxcwWHc5jzmnvh`
- Git 留痕提交过：`9e83061` / `docs: add abi onboarding and shared-source model`。

## Feishu 群协作与 @ 限制
- 已确认群 `锵锵三人行` 的 chat_id 为 `oc_80766d1386e90240e5706495dcac1d4f`。
- 可正常向该群发消息和分享文档链接。
- 但当前 Feishu/OpenClaw 可见目录里，该群只显示 Jeff 一个成员，无法解析阿比在群里的成员 open_id，因此**不能做飞书原生 @阿比**。
- 已采取替代方案：在群里直接文字点名“阿比”，并明确说明这是 Jeff 的安排。

## Abi 私聊触达
- 用户提供的 `oc_d611c3d0e85160e8cbfc2e8d755fb83d` 不是可用于 `sessions_history` 的 OpenClaw session key（查询返回 no session found）。
- 但该 ID 可以作为 Feishu 目标直接发送消息；已成功向 `chat:oc_d611c3d0e85160e8cbfc2e8d755fb83d` 单独发送给阿比。
- 结论：若群里无法原生 @ 阿比，可直接使用该 Feishu 目标做单独提醒。

## Feishu 文档给 Bot 的实践结论
- “把 Feishu 云文档卡片分享到群里”不等于另一个 Bot 一定能直接读取正文。
- 对 Bot 更稳的方式是：
  1. 直接用 doc_token + Feishu 文档读取能力/API 读取；或
  2. 提供纯文本/Markdown 镜像。

## OpenClaw 版本与更新
- 当前机器版本：`OpenClaw 2026.3.8 (3caab92)`。
- GitHub 最新 release 也是 `v2026.3.8`，当前机器已是最新，无需升级。
- 本次检查到的值得注意更新：
  - `openclaw backup create` / `openclaw backup verify`
  - `talk.silenceTimeoutMs`
  - Brave web search `llm-context` 模式
  - ACP provenance (`openclaw acp --provenance off|meta|meta+receipt`)
