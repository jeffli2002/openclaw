# 今日工作日志

## 2026-03-08

### EvoMap 拉取修复

- 问题：Coding Agent 报告 "无新报告"
- 根因：
  1. evomap_auto_pull.sh 缺少 `node_secret` 认证
  2. JSON 格式错误 (`status: "promfilters": {"oted"}`)
  3. 数据路径错误 (读取 `payload.capsules` 而非 `payload.results`)
  4. 缺少去重机制
- 修复：
  - 添加 `Authorization: Bearer <node_secret>` 认证头
  - 修正 JSON 结构
  - 添加 credentials 持久化 (`/root/.openclaw/credentials/evomap.json`)
  - 添加 processed.json 去重
- 结果：成功拉取 16 个新胶囊

### Vercel 构建失败

- 项目：github.com/jeffli2002/2ndbrain
- 错误：`No Next.js version detected`
- 原因：package.json 缺少 `next` 依赖声明
- 状态：待修复

### 每日记忆提取 (08:04)

- strategic.md 02:00 已更新
- 新增：Product Agent workspace 文件缺失问题（已修复）
- 10:04 定时提取：
  - ✅ 提取 Skill 路径管理机制经验
  - ✅ 提取飞书凭据配置问题
  - ✅ 提取模型切换信息
  - ✅ 已写入 strategic.md

### 用户指令

- 要求修复 EvoMap 拉取流程 (已完成)
- 要求手动执行 EvoMap 拉取 (已完成)

### 飞书多维表格写入问题

- 问题：写入记录时报错 `FieldNameNotFound`
- 原因：使用了错误的字段格式
  - ❌ 使用 field_id (如 `fldhqf2zi3`)
  - ✅ 使用中文字段名 (如 `案例名称`)
- 修复：正确使用中文字段名作为 key
- 结果：成功写入 9 条案例记录到多维表格

### product-competitor-analysis 任务修复

- 问题：Product Agent workspace 缺少必要文件
- 错误：
  1. `memory/daily/2026-03-08.md` 不存在
  2. `memory/agents/user/memory.md` 不存在
  3. message 工具发送失败 (缺少 target)
- 修复：
  - 创建 `~/.openclaw/workspace-product/memory/daily/` 目录
  - 复制 user/memory.md 到 product workspace
  - 将 cron 配置与 job payload 改为：若需主动通知老板，必须显式使用 `channel=feishu` + `target=user:ou_aeb3984fc66ae7c78e396255f7c7a11b`
  - 将该 job 的 delivery 从 `announce` 改为 `none`，与 `summary_only` 策略对齐
- 结果：任务修复后运行成功 (ok)

### 2nd Brain 修复

- **Vercel 构建**
  - 本地执行 `npm ci && npm run build` 验证通过
  - 在 `next.config.ts` 显式加入 `outputFileTracingRoot: process.cwd()`，避免多 lockfile / workspace 环境下 Vercel 误判根目录
- **搜索框报错**
  - 根因：前端搜索直接对可能为 null 的字段调用 `.toLowerCase()`
  - 修复：统一改为 null-safe 搜索匹配函数
- **Agents Token 图表缺失**
  - 根因：页面过度依赖本地 cron 历史文件；Vercel 环境下可能拿不到
  - 修复：`/api/token-trend` 新增 Supabase snapshot fallback，Agents 页面新增“当前 Token 分布图”兜底

## 流程固化 (2026-03-08)

### 微信公众号文章抓取流程
**触发**：收到微信文章链接
**流程**：
1. 直接调用 `/root/.openclaw/workspace/scripts/wechat-article <URL>`
2. 读取生成的 Markdown 文件
3. **直接在飞书回复内容给用户**（不只是给路径）
4. 如有图片，列出图片保存路径

**教训**：不要只给路径，要直接展示内容给用户看。

### KIE AI 生图配置

- 用户提供 `KIE_API=5d49452181b5a4c13713c88356610ee3`
- 已写入 `/root/.openclaw/credentials/kie.json`
- 默认配置：
  - model: `nano-banana-2`
  - resolution: `1K`
  - aspect_ratio: `16:9`
  - output_format: `PNG`
- API 请求结构确认：
  - Root: `{ model, callBackUrl?, input }`
  - `callBackUrl` 为可选；若提供，任务完成后会 POST 回调到指定地址
  - 回调内容结构与 Query Task API 响应一致
- 当前结论：KIE 走异步任务 + callback 模式，不是简单同步返回图片
- 13:31 已实测跑通官方端点：`POST https://api.kie.ai/api/v1/jobs/createTask`
- 已验证回调：使用本地 `scripts/kie_callback_server.py` + Cloudflare Tunnel 可收到成功回调
- 成功任务：`taskId=5c0136c899be791169021318b2464d87`
- 回调结果字段：`data.resultJson`，其中包含 `resultUrls[]`
- 下载结果图成功：`temp/images/kie-openclaw-0308-cover.png`
- 已集成进 PPT 生成 Skill：`/root/.agents/skills/baoyu-slide-deck/`
- 新增脚本：
  - `scripts/kie-callback-server.py`
  - `scripts/kie-create-task.py`
  - `scripts/kie-wait-download.py`
- 新增参考文档：`references/image-generation-kie.md`
- 已完成真实自测：`taskId=117326d3b014189c2c7d0ac37708dbe7`
- 自测输出：`temp/images/kie-slide-deck-selftest.png`
- 关键经验：KIE 图床下载用 `urllib` 可能遇到 403，已在等待下载脚本里加入 `curl -L` fallback
### 每日记忆提取 (04:05)

- ✅ 读取昨天+今天 daily memory
- ✅ 提炼3条长期信息写入 strategic.md:
  1. 飞书 Bitable 写入必须用中文字段名
  2. KIE AI 生图异步链路（curl -L fallback）
  3. 2nd Brain 云端三层设计原则

### OpenClaw 新闻监控 (06:00)

**版本**: 2026-03-08 发布
**性质**: 功能更新，无安全问题

**核心新功能**:
1. Context Engine Plugin - 新插件槽位，支持生命周期钩子
2. ACP 持久通道绑定 - Discord/Telegram 线程绑定可跨重启
3. Web UI i18n - 新增西班牙语支持
4. Web Search - 切换到 Search API，支持语言/区域/时间过滤
5. Docker 扩展依赖 - 容器构建可预装扩展
6. Compaction 改进 - 生命周期事件 + 后置上下文配置
7. TTS - OpenAI 兼容端点支持
8. Cron 任务快照 - 故障恢复保留备份

**结论**: 常规功能更新，可纳入每日汇报


### 每日记忆提取 (06:05)
- 上次 04:05 已完成提取
- 今天尚无新工作记录需要提炼
- 检查完毕，跳过

### OpenClaw 动态监控 (08:00)

**版本发布**: OpenClaw 2026.3.2 (2026-03-08)

**核心新功能**:
1. Context Engine 插件接口 - 支持自定义上下文管理策略
2. ACP 持久频道绑定 - Discord/Telegram 持久化
3. Telegram 主题路由 - 支持按主题绑定专属 Agent
4. Web UI i18n - 西班牙语支持
5. Web Search - 改用 Perplexity Search API
6. TTS OpenAI 兼容端点
7. Cron 任务快照持久化 - 增强恢复能力

**建议**: 近期安排升级到最新版本


### 每日记忆提取 (08:04)
- 今天暂无新工作记录（daily文件未生成）
- 上次提取: 06:05

### TrustMRR双人对谈播客 (09:01)

**技术方案**：
1. 角色定义：小李(男声-Yunxi) vs 老王(女声-Xiaoxiao)
2. Edge TTS分角色生成独立ogg片段
3. ffmpeg合并 - 关键：用  编码（不是vorbis）
4. 飞书发送：asVoice=true + mimeType=audio/ogg

**问题解决**：
- 杂音原因：ffmpeg默认用vorbis编码，飞书不支持
- 修复：强制转Opus编码

**Skill位置**：/workspace/skills/trustmrr-podcast/SKILL.md

**后续**：可整合到cron任务，每天8点自动生成播客


### TrustMRR双人对谈播客 (09:01)

**技术方案**：
1. 角色定义：小李(男声-Yunxi) vs 老王(女声-Xiaoxiao)
2. Edge TTS分角色生成独立ogg片段
3. ffmpeg合并 - 关键：用 `-c:a libopus` 编码（不是vorbis）
4. 飞书发送：asVoice=true + mimeType=audio/ogg

**问题解决**：
- 杂音原因：ffmpeg默认用vorbis编码，飞书不支持
- 修复：强制转Opus编码

**Skill位置**：/workspace/skills/trustmrr-podcast/SKILL.md

**后续**：可整合到cron任务，每天8点自动生成播客
### OpenClaw 最新动态 (09:30)

**新版本发布**: v2026.3.7 (2026-03-08)

**核心更新**:
- 新增 ContextEngine 插件接口
- 支持生命周期钩子 (bootstrap, ingest, assemble, compact等)
- 支持 scoped subagent runtime
- sessions.get 网关方法

**影响**: 为 lossless-claw 等插件提供支持

**文档更新**: 待确认


### 每日记忆提取 (10:05)
- ✅ 已提取并写入 strategic.md:
  1. TrustMRR双人对谈播客技术方案
  2. OpenClaw v2026.3.7 动态


### 每日记忆提取 (12:04)
- 今天信息已在 10:05 提取完毕
- 跳过


### 每日记忆提取 (14:05)
- 无新增信息（上次10:05已提取）
- 跳过


### 每日记忆提取 (16:05)
- 微信公众号OCR技术：Tesseract + chi_sim+eng 语言包
- 飞书日历API：calendar:calendar.event:read + calendar:calendar:readonly 权限
- 飞书云文档图片上传：已验证可行
- 跳过（上次已提取）


### 微信公众号OCR技术沉淀 (16:09)

**技术方案**：
- Tesseract OCR 引擎
- chi_sim+eng 语言包混合识别
- OpenCV 图像预处理（灰度+Otsu阈值）
- PSM 6 参数（统一文本块）

**安装**：
```bash
apt-get install -y tesseract tesseract-ocr
wget -O /usr/share/tesseract/tessdata/chi_sim.traineddata \
  https://github.com/tesseract-ocr/tessdata/raw/main/chi_sim.traineddata
pip install pytesseract opencv-python pillow
```

**Skill位置**：/workspace/skills/wechat-article-ocr/SKILL.md

**注意事项**：
- 图片模糊/有水印识别率低
- 微信图片建议先预处理
