# 微信公众号API发布经验总结

**最后更新**: 2026-03-06

## 问题背景

在发布公众号文章时，遇到脚本无法自动读取凭据的问题。

## 解决方案

### 1. 凭据配置位置

- **正确位置**: `/root/.openclaw/workspace/skills/content-factory/.env`
- **凭据来源**: `/root/.openclaw/credentials/wechat.json`

### 2. 配置步骤

```bash
# 读取凭据
cat /root/.openclaw/credentials/wechat.json | jq -r 'to_entries | map("\(.key)=\(.value)") | join("\n")'

# 写入 .env 文件
# WECHAT_APP_ID=wxf9400829e3405317
# WECHAT_APP_SECRET=f122506bb959353211175841f8ed039d
```

### 3. 正确使用方式

**错误方式**:
```bash
# ❌ 在命令行临时设置环境变量
WECHAT_APP_ID=xxx python3 script.py
```

**正确方式**:
```bash
# ✅ 直接运行脚本（会自动读取 .env）
cd /root/.openclaw/workspace/skills/content-factory
python3 -X utf8 scripts/wechat_publish.py --html "path/to/article.html"
```

### 4. 发布命令

```bash
cd /root/.openclaw/workspace/skills/content-factory
python3 -X utf8 scripts/wechat_publish.py --html "/root/.openclaw/workspace/output/2026-03-06-xxx.html"
```

**注意**: 必须加 `-X utf8` 参数，否则中文字符会变成 `\uXXXX` 编码

### 5. API 权限限制

- ✅ 获取access token: 成功
- ✅ 上传封面图片: 成功
- ✅ 创建草稿(draft): 成功
- ❌ 提交预览(preview): API未授权（需要更高权限）

**结果**: 草稿会自动创建，需登录 https://mp.weixin.qq.com/ 手动发布

## 关键要点

1. **凭据写入 .env**: Content Factory Skill的脚本会自动读取 `skills/content-factory/.env` 文件
2. **使用 -X utf8**: Windows环境下必须加这个参数防止编码问题
3. **手动发布**: 由于API权限限制，草稿创建后需手动在后台发布

## 重大错误：价格数据编造问题

**时间**: 2026-03-06

**错误描述**:
在撰写GPT-5.4 vs Claude对比文章时，我编造了GPT-5.4的API价格数据。

**错误数据**: $30/M (输入) / $180/M (输出)
**正确数据**: $2.5/M (输入) / $15/M (输出) - 来自platform.openai.com

**错误原因**:
1. 没有第一时间从官方网站核实数据
2. 主观猜测价格数据
3. 写完文章后才去查证，而不是之前

**深刻反思**:
- ❌ 禁止在文章中编造任何数据
- ❌ 价格、参数、排名等信息必须来自官方或权威来源
- ✅ 必须先查证，再写作
- ✅ 如果不确定，明确标注"待核实"

**正确流程应该是**:
1. 确定主题
2. **先查证所有数据**（官网、权威报道）
3. 整理信息
4. 写作
5. 再次核实

## 待核实问题

1. **OpenClaw是否支持GPT-5.4**: 从GitHub看到支持OpenAI，但未明确列出GPT-5.4，需进一步确认
2. **GPT-5.4订阅方式**: $20/月起的价格来源待核实

## 相关文件

- 凭据: `/root/.openclaw/credentials/wechat.json`
- .env: `/root/.openclaw/workspace/skills/content-factory/.env`
- 发布脚本: `/root/.openclaw/workspace/skills/content-factory/scripts/wechat_publish.py`
- HTML模板规范: Content Factory SKILL.md

---

## 2026-03-06 下午：GPT-5.4 vs Claude 文章及小红书发布

### 微信公众号文章发布
- 主题：GPT-5.4发布，最强AI大脑来了，OpenClaw配它才更香
- 修改次数：4次（根据老板反馈不断调整）
- 最终状态：草稿已创建，需手动发布

### 关键修改点
1. **核心逻辑**：OpenClaw是AI Agent框架，需要配"大脑"（大模型）
2. **Claude Code关系**：Pro订阅是桌面应用，和OpenClaw里用是两回事
3. **GPT-5.4价格**：$20/月起（不是$200）
4. **API支持**：GPT-5.4有官方API（$2.5/M输入/$15/M输出）
5. **模型名称**：GPT Codex 5.3（不是GPT-5.3-Codex）

### 小红书发布
- 标题：OpenClaw最强大脑GPT5.4发布（20字符，含OpenClaw关键词）
- 文案：兄弟们开头（不是姐妹们），针对AI爱好者
- 状态：封面图已准备，但发布失败（浏览器自动化问题）

### 技术问题
1. **小红书发布失败原因**：
   - 服务器环境无X server，无法运行图形界面Chrome
   - Headless模式无法完成小红书登录验证（需要滑动验证码）
   - 建议：需要本地有GUI的电脑来运行

### 经验教训
- 写文章前必须先核实所有数据，禁止编造
- 价格信息必须来自官网（platform.openai.com, anthropic.com）
- 小红书受众是AI爱好者，男性为主，文案要去"姐妹们"

---

## 2026-03-06 14:30 飞书语音 Skill 固化

### 已完成
- 将飞书语音播放能力固化为 Skill：`skills/feishu-voice-reply/`
- 已创建脚本：`scripts/build_feishu_voice.py`
- 技术链路验证通过：Edge TTS → ffmpeg → Ogg/Opus → Feishu 语音播放条
- 已打包：`dist/skills/feishu-voice-reply.skill`
- 统一本地生效路径：`/root/.openclaw/workspace/skills/feishu-voice-reply`

### 模型配置更新
- Main / Coding：GPT-5.4 → Minimax M2.5 → Kimi 2.5
- 其他 Sub Agent 默认：Minimax M2.5 → Kimi 2.5

### 备注
- Skill 内容已检查，无私人敏感信息、无凭据、无 Token
- 若需发布到 ClawHub，当前阻塞点是本机 `clawhub` 尚未登录，需要先完成 `clawhub login`

---

## 14:20 新增记录

### 飞书语音播放能力固化为 Skill
- 需求：老板要求“语音播放”时，不是发 mp3 文件路径，而是在飞书里直接展示可播放/暂停的原生播放条
- 验证通过链路：**Edge TTS → ffmpeg 转 Ogg/Opus → Feishu asVoice 发送**
- 已创建 Skill：`skills/feishu-voice-reply/`
- 核心脚本：`skills/feishu-voice-reply/scripts/build_feishu_voice.py`
- 打包产物：`dist/skills/feishu-voice-reply.skill`
- 结论：后续在飞书里做语音播放，默认同时发送**文本 + Ogg/Opus语音消息**

### Agent 模型分工更新
- Main / Coding：`openai-code/gpt-5.4` → `minimax-cn/MiniMax-M2.5` → `kimi-coding/k2p5`
- 其他 Sub Agent 默认：`minimax-cn/MiniMax-M2.5` → `kimi-coding/k2p5`
- 纠偏：旧记忆里“Chief/Content/Coding 用 MiniMax、Growth/Product/Finance 用 Kimi”的结论已过期，不再适用


---

## Chief 路由机制整改（2026-03-06 19:06）

### 已完成
- 修正 `AGENTS.md`：不再把“假定存在的 sessions_send/session_key”写成已接通链路，改为区分 runtime 真绑定、Chief 私聊分类、委派优先级与降级执行逻辑。
- 重写 `scripts/agent_keyword_router.py`：从简单 best-match 演示脚本升级为可输出候选列表、歧义判断、长任务/紧急标识、执行建议的分类器。
- 重写 `scripts/auto_route.py`：从“打印伪 sessions_send 示例”改为真实的执行规划器，明确当前只负责分类与建议，不冒充已完成 runtime 分发。
- 更新 `agents/chief_agent.py`：改为读取 `openclaw.json` + `config/agent_keyword_router.yaml` 作为真相源，不再使用过时的模型和关键词硬编码。
- 标记冲突文件：
  - `config/group_agent_mapping.yaml` 标记为 reference-only
  - `agents/config.yaml` 标记为 deprecated / 非执行真相源

### 当前结论
- 群聊 -> 对应 Agent：runtime 已生效
- Chief 私聊 -> 关键词分类：已具备、已增强
- Chief 私聊 -> 自动真实分发给 Sub Agent：仍需 runtime 层提供稳定 session / spawn 能力，当前尚未完全闭环

### Chief 真实委派实现补充（2026-03-06 19:12）
- 验证发现：当前 Feishu 通道不支持 `thread:true` 的持久 subagent worker，因此“长期挂着的 dispatch thread”目前不可用。
- 可行且已验证接受的真实委派方式：`sessions_spawn(runtime="subagent", mode="run")`，按任务即时拉起一次性 worker。
- 已创建配置文件：`config/chief_dispatch_workers.yaml`
  - content/growth/product/finance -> MiniMax M2.5
  - coding -> GPT-5.4
- 结论：Chief 的下一阶段真实派活方式不是持久 thread，而是按任务即时 spawn worker；未来如果通道支持 thread hooks，再升级为持久 worker 架构。

### Chief 显式回传桥验收（2026-03-06 22:25）
- 已新增 `scripts/wait_dispatch_result.py`，用于等待 worker 将结果写入回传桥文件。
- 已升级 `scripts/chief_dispatch.py`：为每次委派生成唯一 `dispatch_id` 与 `result_file`，并把回传桥要求写进 worker prompt。
- 已更新 `config/chief_dispatch_workers.yaml`：新增 `runtime.result_bridge` 配置。
- 已做完整闭环验收：
  1. Chief 将“公众号选题”任务分类为 `content`
  2. 生成 `delegate_spawn` 计划
  3. 成功 `sessions_spawn` 启动 content worker
  4. worker 将结果写入 `/output/dispatch_results/...md`
  5. Chief 通过 `wait_dispatch_result.py` 读回结果
- 结论：Chief -> Content worker -> 显式回传桥 -> Chief 的闭环已跑通。

### 所有 worker 显式回传桥实战验收（2026-03-06 22:31）
- 对 content / coding / growth / product / finance 五类任务分别执行了：
  1. Chief 分类
  2. delegate_spawn 计划生成
  3. sessions_spawn(mode=run) 启动 worker
  4. wait_dispatch_result.py 读取回传桥结果
- 验收结果：
  - content：通过
  - coding：通过
  - growth：通过
  - product：正文成功返回，但未写头部元信息；后续由 `wait_dispatch_result.py` 使用 expected metadata 自动补齐并成功消费
  - finance：正文成功返回，但未写头部元信息；后续由 `wait_dispatch_result.py` 使用 expected metadata 自动补齐并成功消费
- 已加固 `wait_dispatch_result.py`：支持 `--expected-dispatch-id` / `--expected-agent` / `--expected-route-debug`，当 worker 只写正文时，Chief 仍能稳定消费结果。
- 当前结论：五类 worker 的“分类 -> 派发 -> 结果桥 -> Chief 消费”链路均已跑通；但产品/财务类 worker 对头部协议的遵守仍不如 content/coding/growth 稳定，后续可继续强化 prompt 或改为 JSON 回传格式。

### 强校验契约加固（2026-03-06 22:41）
- 已将 `config/agent_keyword_router.yaml` 增加 `behavior.min_score` 与 `behavior.low_signal_keywords`，用于降低泛词对路由的误导。
- 已重写 `scripts/agent_keyword_router.py` 的匹配策略：
  - 不再对抽取短语做双向子串猜测
  - 英文/技术词按词边界精确匹配
  - 中文按配置关键词原样匹配
  - 低信号关键词只给低权重
  - 排序时优先高信号命中，避免多个低信号词把强领域词压过去
- 已加固 `scripts/wait_dispatch_result.py`：
  - 增加 `--allowed-dir`
  - 增加 `--expected-dispatch-id`
  - 增加 `--expected-agent`
  - 增加 `--expected-route-debug`
  - 只有路径合法且契约校验通过，才返回 `ready=true`
- 已更新 `scripts/chief_dispatch.py`：result_bridge 现在会返回 `allowed_dir`
- 已更新 `AGENTS.md`：等待回传桥时明确要求使用 allowed_dir + expected_* 强校验参数
- 回归验证结果：
  - `/etc/passwd` 会被明确拒绝（path_outside_allowed_dir）
  - Product 历史桥文件在未提供 expected_* 时不会再被误判为成功
  - 在提供 expected_* 后，可以兼容消费历史正文-only 结果
  - `API/Bug` 类 coding 消息现在能正确命中 `API` 与 `bug`
  - `设计一个用户增长策略` 现在优先路由到 growth，而不是被 product 的低信号泛词抢走

### 回传桥升级为 JSON 协议（2026-03-06 22:58）
- 已将 `config/chief_dispatch_workers.yaml` 中的 `runtime.result_bridge.format` 设为 `json`。
- 已重写 `scripts/chief_dispatch.py`：
  - `result_bridge` 默认生成 `.json` 文件
  - worker prompt 默认要求写入 JSON 对象
  - JSON 协议字段：`protocol`, `status`, `route_debug`, `dispatch_id`, `agent`, `body`
- 已重写 `scripts/wait_dispatch_result.py`：
  - 先解析 JSON bridge
  - 保留对 legacy text bridge 的兼容读取
  - 继续强校验 `allowed_dir + expected_dispatch_id + expected_agent + expected_route_debug`
- 已做真实闭环验收：
  1. Chief 生成 `content` 的 JSON bridge 计划
  2. sessions_spawn 启动 content worker
  3. worker 写入 `/output/dispatch_results/*.json`
  4. `wait_dispatch_result.py` 成功读回 JSON 并完成契约校验
- 验收结果：JSON bridge 升级已真实生效，不只是文件后缀修改。

### 任务分配逻辑 cleanup（2026-03-06 23:12）
- 已将 `agents/config.yaml` 归档到 `archive/legacy-routing/agents-config.deprecated.yaml`，原路径改为 compatibility stub，避免继续被误当成执行真相源。
- 已将 `config/group_agent_mapping.yaml` 归档到 `archive/legacy-routing/group_agent_mapping.deprecated.yaml`，原路径改为 reference stub，避免与 `openclaw.json` bindings 形成双真相源。
- 已将 `scripts/auto_route.py` 收敛为兼容 wrapper，统一转发到 `chief_dispatch.py`，避免存在第二套“看起来也能执行”的计划脚本。
- 已更新 `agents/prompts/chief_system.md`，删除旧的理想化私聊路由叙述，改为引用当前真实真相源与执行链路。
- 已更新 `agents/chief_agent.py`：
  - 移除对 legacy config 的显式依赖展示
  - `__main__` 烟雾测试改为 `plan_dispatch()`，避免再生成测试 memory 噪音目录
- 已新增 `docs/chief-dispatch-truth-sources.md`，明确当前唯一真相源与 deprecated 文件清单。
- 已给 `scripts/cron_dispatcher.py` / `scripts/generate_cron_jobs.py` 增加 legacy/compat 标记，避免和 Chief 私聊主链路混淆。
- 已清理测试噪音目录：`memory/global/{chief,coding,content,growth}` 与 `scripts/__pycache__/`。
