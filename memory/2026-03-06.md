## 2026-03-06 14:41 Durable Memory Flush

### Feishu 通道与回复链路
- 飞书此前“消息像没收到”的根因不是接收失败，而是回复阶段默认走流式卡片，应用缺少 `cardkit:card:write` 权限导致报错。
- 在权限开通前，已临时关闭 Feishu streaming/card block；权限开通后已恢复流式卡片回复。
- 验证结论：Feishu 接收链路正常，关键是回复权限与回复模式匹配。

### Agent 模型配置（最新有效版本）
- Main / Coding：`GPT-5.4 -> Minimax M2.5 -> Kimi 2.5`
- 其他 Sub Agent 默认（Content / Growth / Product / Finance）：`Minimax M2.5 -> Kimi 2.5`
- 已修正旧记忆中“Chief/Content/Coding 用 MiniMax、Growth/Product/Finance 用 Kimi”的过时说法；后续以本配置为准。

### Feishu 语音播放实现结论
- 需求是：用户说“语音播放一下……”，系统需同时返回可见文字 + 飞书内可播放/暂停的语音条。
- 失败路线：系统内置 TTS 直接回传飞书时，曾出现空文件/400 参数错误；raw mp3 作为普通附件不能稳定满足飞书原生播放条体验。
- 已验证成功路线：`Edge TTS -> ffmpeg 转 Ogg/Opus -> Feishu 语音消息(asVoice=true, mimeType=audio/ogg)`。
- 关键经验：如果目标是飞书原生播放条，必须走 Opus/Ogg 方向，不能停留在 mp3 文件直发。

### 新增可复用 Skill
- 已创建 Skill：`/root/.openclaw/workspace/skills/feishu-voice-reply`
- 统一安装路径确定为：`/root/.openclaw/workspace/skills/feishu-voice-reply`
- 核心脚本：`scripts/build_feishu_voice.py`
- 功能：把文本合成为中文语音，并转换为 Feishu 兼容的 Ogg/Opus 语音文件。
- 打包产物：`/root/.openclaw/workspace/dist/skills/feishu-voice-reply.skill`
- 安全结论：已检查 Skill 内容，不含私人敏感信息、凭据、Token、Secret，可分享。

### 发布与安装状态
- 本地 Skill 已落在标准 skills 目录，可作为统一安装路径使用。
- ClawHub CLI 已安装，但当前机器尚未登录；发布到 ClawHub 的阻塞点是需要先执行 `clawhub login`。

### 已同步到项目记忆
- `memory/global/strategic.md` 已补充两类长期信息：
  1. 模型分工已正式引入 GPT-5.4
  2. 飞书语音播放链路已沉淀为可复用 Skill
- `memory/daily/2026-03-06.md` 已补记当天的 Skill 固化与模型配置更新。

## 2026-03-06 18:45 Durable Memory Flush

### 用户对 Chief / Coding 响应体验的明确要求
- 用户明确反馈：Chief Agent 与 Coding Agent 在处理复杂任务时不能长时间“像失联”，必须先给可见反馈。
- 期望体验是两层机制：
  1. 前台快速响应（打字指示、短确认、能回简单状态问题）
  2. 后台再处理复杂任务或委派执行
- 用户特别在意：自己中途打招呼时，要立刻有反应，而不是等前一轮大任务跑完才统一回复。

### Chief / Coding 响应性相关改动与规则
- 已把 AGENTS.md 补充为“前台快响应 + 后台重处理”机制：简单问题优先即时回答；复杂任务优先委派；禁止长时间闷头执行后一次性回复。
- 已加入委派决策规则：代码/内容/增长/产品/财务/深度分析类任务，Chief 默认应优先分配给对应领域 Agent，而不是自己硬扛。
- 已显式配置打字提示相关参数：`typingMode=instant`、`typingIntervalSeconds=4`，并确保 Feishu `typingIndicator=true`，用于提升忙时可见反馈。

### 模型偏好与最终用户偏好
- 用户明确要求：Coding Agent 默认应保留 `GPT-5.4`，因为代码质量优先级高于速度。
- 用户随后要求切换回 `GPT-5.4` 模型处理当前 Chief 对话；这一轮偏好说明：Chief 即使承担前台职责，用户仍接受/偏好在关键分析任务中使用 GPT-5.4。
- 因此后续记忆里，不能再简单把“Chief 必须长期固定为 MiniMax”当成最终决策；更准确的理解是：用户要的是“Chief 前台快速反馈 + 重任务可用 GPT-5.4”，而不是单纯换成快模型。

### 关于任务路由机制的最新审计结论
- 当前系统中，“群聊 -> 对应 Agent”的绑定是真实 runtime 生效的，来源于 `openclaw.json` 的 `bindings`。
- `config/agent_keyword_router.yaml` 与 `scripts/agent_keyword_router.py` 已能完成关键词分类；实测复合消息可被路由到 Coding 等目标类别。
- 但“Chief 私聊收到任务后，自动把任务真实发送给 Sub Agent 并等待结果回流”这条链路尚未完全打通：
  - `AGENTS.md` 中的自动路由更像行为规范，不是平台原生钩子
  - `scripts/auto_route.py` 目前只输出建议的 `sessions_send(...)`，并不会真的执行分发
  - 当前可见 session 里只有 main，尚无稳定可复用的 content/growth/coding/product/finance 持久 session key
  - 当前 `agents_list` 仅暴露 `main`，说明 Chief 不能直接按预想方式把其他领域 Agent 当成可随时 spawn 的 subagent 使用
- 结论：现在的路由体系是“规则和脚本已具备、群聊绑定已具备，但 Chief 私聊自动真实分发链路尚未完全闭环”。

### 路由配置冲突与后续改进方向
- 当前至少存在 5 份相关配置/说明源：`openclaw.json`、`AGENTS.md`、`config/agent_keyword_router.yaml`、`config/group_agent_mapping.yaml`、`agents/config.yaml`。
- 审计结论：
  - `openclaw.json` 是最接近真实 runtime 的配置源
  - `AGENTS.md` 主要是行为规则
  - `config/agent_keyword_router.yaml` 是分类规则源
  - `group_agent_mapping.yaml` 与 runtime bindings 有重复定义风险
  - `agents/config.yaml` 明显过时且局部格式损坏，不适合继续作为可信执行依据
- 关键词本身存在重叠风险（如“脚本”“分析”“发布”），当前实现是单 best-match，不擅长处理真正的复合任务。
- 建议的长期整改方向：
  1. 统一“唯一真相源”
  2. 把分类结果真正接到可执行的 `sessions_send` / `sessions_spawn` 链路
  3. 明确复合任务拆分规则
  4. 下线或标记废弃历史配置文件，避免未来继续冲突
