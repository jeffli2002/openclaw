## 00:43 小红书登录/发布链路 durable memory
- Feishu 发图片问题已定位：当图片源在 `/tmp/...` 等非允许目录时，OpenClaw/Feishu 发送会退化为发送“路径文本”；将图片复制到 workspace 内并用 `message(media=...)` 发送后，二维码图片可正常投递到 Feishu。
- 小红书远端扫码登录在当前 Linux/Xvfb 远端浏览器环境下不稳定：用户多次扫码后会出现 `fail to log in`，自动化侧也表现为登录超时，因此不要把“远端扫码成功”当作可靠方案。
- 本地 Chrome Browser Relay 当晚无法正常附着，因此改为“用户手工提供本地已登录 cookies”的降级方案。
- 通过用户提供的小红书 cookies（核心至少含 `a1`、`web_session`，并配合 creator 相关 token）注入远端 Chrome 的 CDP `Network.setCookies` 后，已能直接进入 `https://creator.xiaohongshu.com/publish/publish?source=official`，且可见“上传图文 / 写长文 / 上传区域”。
- 对后续小红书自动发布测试，应以“creator 发布页是否可进入、上传区域是否可见”作为登录成功判据；现有 `check-login` 基于 explore 页旧选择器，可能误报未登录，不应再作为唯一真相源。
- 当前可复用的小红书 cookies 已备份到 `/root/.openclaw/credentials/xiaohongshu.json`，供后续 creator 发布恢复与图文发布半链路测试使用。

## 08:24 AI日报 cron / Content Agent 调度 durable memory
- 2026-03-07 7:30 的 `ai-daily-newsletter` 漏发，根因不是单纯模型/内容生成失败，而是当时活跃的 `/root/.openclaw/cron/jobs.json` 一度为空（07:49 备份出的 `jobs.json.pre_restore_20260307_0749` 只有 32 字节、0 个 job），导致调度器在 07:30 根本没有这条任务可执行。
- 用户明确指出 3 个严重问题：① AI日报未按时发送且系统未主动告警/补救；② 执行时没有调用 `skills/ai-daily-newsletter/SKILL.md` 且未遵守 `config/ai-daily-format.md`；③ 任务本应优先委派给 Content Agent，但实际被 Chief 直接处理。这 3 条应视为后续 AI 日报链路的硬约束。
- 已将线上 cron 重建为走 `sessionTarget: main` 的 main-session systemEvent，而不是旧的 `sessionTarget: isolated` agentTurn。这样任务会先进入 Chief 主会话，再按关键词/规则路由给对应 Agent；内容类任务不应再直接在 isolated cron worker 内裸跑。
- 已重建并生效的关键任务：`ai-daily-newsletter`（07:30，ID `3e36cb70-5b1e-450e-9605-31513fd0509f`）、`daily-content-publish`（09:00，ID `c8f7e782-5b7a-4e69-ba52-b6820ce63891`）、`ai-daily-delivery-guard`（07:35，ID `a7de1af8-10be-4ab5-8c71-e07c6814df20`，用于漏发守卫）、`chief-daily-report`（19:30，ID `b4ed7b05-2583-4940-abd4-1c8eb926cf63`）。这些任务当前均已写回 cron store。
- `ai-daily-newsletter` 的 systemEvent 文本已显式写入：目标 Agent=content、必读 skill=`/root/.openclaw/workspace/skills/ai-daily-newsletter/SKILL.md`、必读格式=`/root/.openclaw/workspace/config/ai-daily-format.md`、并要求“必须优先委派给 Content Agent，Chief 仅在委派失败时降级处理”。
- `daily-content-publish` 也已改为 main-session 路由，并在任务文本中写死“必须使用 content-factory skill，优先委派给 Content Agent”。
- `config/cron-agent-dispatch.yaml` 与 `scripts/generate_cron_jobs.py` 已同步更新：支持从配置里声明 `session_target`、`required_skill`、`required_format`、`message_instructions`，并把这些约束自动注入 cron 消息，避免以后 снова 只发一个模糊的“AI日报 newsletter”短提示。
- 这次修复已提交并推送到 GitHub：commit `044fa8e`（`fix: 修复AI日报cron任务配置`）。后续若 AI 日报再出问题，应先检查 cron store 是否被清空、任务是否仍是 `sessionTarget: main`、以及 systemEvent 中的 skill/format 约束是否还在。

## 08:41 Cron owner-agent 直达改造
- 用户确认采用“固定归属 cron 直接到 owner agent，Chief 只做监督/兜底/汇总”的方向，并要求立刻执行。
- 已将 `ai-daily-newsletter` 与 `daily-content-publish` 两个线上关键任务直接切换为：`sessionTarget: isolated` + `agentId: content` + `payload.kind: agentTurn` + `delivery.mode: announce`。不再先进入 Chief 主会话再二次分发。
- 已现场核对：`openclaw cron list` 中这两个任务现在都显示为 `Target=isolated`、`Agent ID=content`。
- 已重构 `/root/.openclaw/workspace/scripts/generate_cron_jobs.py` 为 v3：支持按 `config/cron-agent-dispatch.yaml` 的 `dispatch_mode` / `session_target` 生成两类任务——固定归属任务直达 owner agent；监督兜底类任务继续走 main session。
- 已更新 `config/cron-agent-dispatch.yaml`：把内容/增长/产品/代码等固定归属任务标记为 `dispatch_mode: direct_agent`，其中 AI Daily News 与 Daily Content Publish 明确使用 `session_target: isolated`；Chief 相关守卫/汇总任务保留 main-session 语义。
- 为防回滚困难，已先备份 cron store：`/root/.openclaw/cron/jobs.json.pre_direct_owner_20260307_083931`。

## 08:46 Cron 全量归属梳理 + 批量改造
- 用户确认“按默认建议改”：`sync-supabase -> coding`、`github-daily-backup -> coding`、`daily-memory-extractor -> chief`、`cron-health-check -> chief`。
- 已更新 `config/cron-agent-dispatch.yaml`：补充 `github-daily-backup`、`sync-supabase-*-*` 的 owner agent= coding；明确 `daily-memory-extractor` 与 `cron-health-check` 归 Chief，走 main session；并给 growth/product/coding 相关任务补了 direct-owner message instructions。
- 已将线上 cron 全量对齐到新归属：
  - Content: `ai-daily-newsletter`、`daily-content-publish`、`ai-kol-daily-newsletter`
  - Growth: `openclaw-news-monitor`
  - Product: `product-competitor-analysis`
  - Coding: `github-daily-backup`、`sync-github-*`、`sync-supabase-*`、`daily-skill-evolution`
  - Chief(main/systemEvent): `ai-daily-delivery-guard`、`chief-daily-report`、`daily-memory-extractor`、`cron-health-check`
- 因 OpenClaw CLI 限制，`daily-memory-extractor` 和 `cron-health-check` 不能直接从 `isolated/agentTurn` 原地 patch 到 `main/systemEvent`；已改为“按新配置重建 job 再删除旧 job”的方式完成迁移。
- 已清理重复的 GitHub 同步任务：删除了重复的 `sync-github-12-00`、`sync-github-15-00`、`sync-github-18-00`、`sync-github-21-00` 各 1 条，cron 总数从 25 降到 21。
- 当前仍需注意：5 条 `sync-supabase-*` job 本身处于 `enabled=false` 状态，这是历史状态遗留，不是这次改造新增；如后续确认需要启用，再单独开启即可。

## 09:03 daily-content-publish 故障定位 + 巡查机制增强
- 用户反馈“9点 Cron job 还没有执行”。实际排查结果：`daily-content-publish` 已在 09:00:00 触发（`runAtMs=1772845200007`），但 09:02 左右以 `error` 结束，根因是 cron delivery 缺少 Feishu 明确目标：`Delivering to Feishu requires target <chatId|user:openId|chat:chatId>`。因此表现为“任务跑了，但老板没收到内容”。
- 该问题只影响手工改造后的 `daily-content-publish` 与 `ai-daily-newsletter`：两者的 `delivery` 只有 `mode=announce`，缺少 `channel/to`；而其他旧任务仍保留 `channel=last`，所以能正常投递。
- 已修复：
  - `daily-content-publish` 增加 `delivery.channel=feishu`、`delivery.to=user:ou_aeb3984fc66ae7c78e396255f7c7a11b`
  - `ai-daily-newsletter` 同样增加明确 Feishu 投递目标
  - 以上两条关键任务都补了 `failureAlert(after=1, channel=feishu, to=user:ou_aeb3984fc66ae7c78e396255f7c7a11b)`
  - `ai-daily-delivery-guard` 与 `cron-health-check` 也补了 failure alert，避免巡查任务自己失败时没人知道
- 已立即测试：手动 rerun `daily-content-publish` 后，最新 run 变为 `status=ok`、`deliveryStatus=delivered`，说明投递链已修好；内容任务已成功向老板发出候选选题消息。
- 已新增巡查增强：创建 `daily-content-publish-guard`（09:05，ID `e175b13e-ea8d-4eb5-9468-d3e8f1cf61fb`），由 Chief 在内容发布后 5 分钟检查今天的 `daily-content-publish` 是否成功执行并投递；若失败/漏发，立即补跑并汇报。该守卫任务也已配置 failure alert。
- 当前 cron 巡查机制升级后为：
  1. `ai-daily-delivery-guard`（07:35）负责 AI 日报补发守卫
  2. `daily-content-publish-guard`（09:05）负责内容发布补跑守卫
  3. `cron-health-check`（每2小时）负责全量巡检 lastStatus / nextRunAtMs / delivery / 重复任务
  4. 关键 P0 任务本身附带 failure alerts，失败后直接通知老板

## 09:24 Cron 通知策略统一改造
- 用户要求统一整理所有 cron 的 delivery / alert 策略：高频 GitHub 类任务不要每次打扰老板，但一旦失败必须 alert；同时每日要有一份总汇报（可通过 `chief-daily-report`），任何需要老板确认的事项必须即时通知。
- 已将该规则写入 `/root/.openclaw/workspace/config/cron-agent-dispatch.yaml`，新增显式 `notify_policy`：
  - `deliver_and_alert`：关键任务直接显式投递到 Feishu，同时失败 alert
  - `summary_only`：不逐次打扰老板，摘要进入 Chief 每日汇报，同时失败 alert
  - `failure_only`：不常规投递，只在失败时 alert
- 已升级 `/root/.openclaw/workspace/scripts/generate_cron_jobs.py` 为 v4，支持根据 `notify_policy` 自动生成显式 `delivery` / `no-deliver` / `failureAlert` 配置，并默认使用 `message_delivery.default_channel=feishu`、`default_target=user:ou_aeb3984fc66ae7c78e396255f7c7a11b`，不再依赖 `channel:last`。
- 已将线上 21 条 cron 全量对齐到新通知策略：
  - **显式直达 Feishu + failure alert**：`ai-daily-newsletter`、`daily-content-publish`、`chief-daily-report`
  - **不常规投递、仅汇总/失败告警**：`github-daily-backup`、`sync-github-*`、`sync-supabase-*`、`ai-kol-daily-newsletter`、`product-competitor-analysis`、`daily-skill-evolution`、`openclaw-news-monitor`
  - **main supervisor，仅 failure alert**：`ai-daily-delivery-guard`、`daily-content-publish-guard`、`daily-memory-extractor`、`cron-health-check`
- `chief-daily-report` 已从 `main/systemEvent` 重建为 `isolated + agent=main + explicit Feishu delivery`，并在任务说明中强制要求汇总 GitHub / Supabase / OpenClaw / 待确认事项摘要，作为老板每日总报入口。
- 已现场核对：当前所有需要发给老板的 cron 都有明确 `delivery.channel=feishu` + `delivery.to=user:...`；所有不需要常规打扰的任务都已改成 `delivery.mode=none` 并补齐 `failureAlert -> Feishu user`。
