# 2026-03-12

## Durable memories
- 早上 07:30 的 `ai-daily-newsletter` 没有按预期送达；排查时确认一部分影响来自 07:32 左右的 gateway restart 打断了关键时间点，同时原 `ai-daily-delivery-guard` 存在跨天误判：会把前一天成功记录当成“今天已发送”。
- 今天的 AI 日报已手工补发，并落地到本地文件：`reports/ai-daily-2026-03-12.md`。
- 已修复并重建 AI 日报主链路：
  - 新 `ai-daily-newsletter` cron id: `8cfba7f1-0af4-48e0-bfa5-05015e2fdfa5`
  - 新 `ai-daily-delivery-guard` cron id: `5c135179-8c5e-42a8-996d-b15ae458aa96`
  - 修复内容：把日报 timeout 从 900 秒提升到 1800 秒；守卫任务说明改为必须按 Asia/Shanghai 当天 run + `deliveryStatus=delivered` 验证，不能只看 `lastStatus`。
  - 已删除旧 cron：`3e36cb70-5b1e-450e-9605-31513fd0509f`、`a7de1af8-10be-4ab5-8c71-e07c6814df20`。
- 今天 09:00 的 `daily-content-publish` 实际运行成功：09:00:55 启动，`status=ok`，`deliveryStatus=delivered`；内容为今日热点 + 4 个选题候选。09:05 守卫确认无需补跑。
- 截至 09:21，仍需后续排查的失败 cron 有 4 条：
  - `cron-health-check`：delivery JSON 解析错误（`Unexpected non-whitespace character after JSON...`）
  - `chief-daily-report`：超时
  - `daily-skill-evolution`：超时
  - `github-daily-backup`：error（待进一步确认根因）
- 今日心跳检查中确认 OpenClaw 最新 release 为 `v2026.3.8`；和当前运维最相关的更新包括 cron 调试说明（`cron run` 仅入队，需用 `cron runs --id` 跟结果）以及新增 backup/create/verify 等能力。

- 老板新增内容偏好：每天 09:00 的 `daily-content-publish` 选题需要重点关注 OpenClaw 相关内容，重点方向包括创业机会、版本更新、使用玩法、相关 skill、使用场景；研究来源优先抓 YouTube 和 Twitter/X；3-5 个候选主题里 OpenClaw 相关条目不少于 3 条。
